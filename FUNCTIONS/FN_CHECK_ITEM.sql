DELIMITER $$


CREATE  FUNCTION `FN_CHECK_ITEM`(items VARCHAR(30)) RETURNS INT(11)

BEGIN
	
IF EXISTS(SELECT ITEM FROM seed_food WHERE ITEM=items) THEN
	
RETURN 1;
	
ELSE
	
RETURN 0;
	
END IF;
    
END$$

DELIMITER ;